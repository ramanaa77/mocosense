import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const primary = Color(0xFF0F766E);
  static const primaryDark = Color(0xFF115E59);

  // Security status
  static const success = Color(0xFF16A34A);
  static const warning = Color(0xFFD97706);
  static const critical = Color(0xFFDC2626);
  static const info = Color(0xFF2563EB);

  // Light theme
  static const lightBackground = Color(0xFFF8FAFC);
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightCard = Color(0xFFFFFFFF);
  static const lightBorder = Color(0xFFE2E8F0);

  // Dark theme
  static const darkBackground = Color(0xFF070B0A);
  static const darkSurface = Color(0xFF0D1311);
  static const darkCard = Color(0xFF0B1110);
  static const darkBorder = Color(0xFF202B27);
}
